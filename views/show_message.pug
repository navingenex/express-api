html
    head
        title Person
    body
        if(type=="error")
            h3(style="color:red") #{message}
        else
            h3 New person
               span  #{person.name} #{person.age} and #{person.nationality}
                
